name: Deploy to GitHub Pages

on:
  push:
    branches: [ epetrashko-deploy-hw5 ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Install and Build
        run: |
          npm install
          npm run build
